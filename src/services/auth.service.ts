import { Injectable } from '@angular/core';

// This service is intentionally left empty as authentication 
// has been removed in favor of local persistence.
@Injectable({
  providedIn: 'root'
})
export class AuthService {
  // No-op
}